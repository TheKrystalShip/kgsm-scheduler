{
  "schemaVersion": 1,
  "id": "scheduler",
  "displayName": "Scheduler",
  "unit": "kgsm-scheduler.service",
  "role": "Runs each server's maintenance windows — backups and restarts, in order, announced and exclusive — and sweeps the roster for newer game builds.",
  "onDemand": false,
  "applyMode": "restart",
  "floorSources": [
    {
      "kind": "appsettings",
      "path": "/opt/kgsm-scheduler/kgsm-scheduler.settings.json"
    },
    {
      "kind": "systemd-unit",
      "path": "kgsm-scheduler.service"
    },
    {
      "kind": "env-file",
      "path": "/etc/kgsm-scheduler/kgsm-scheduler.env"
    }
  ],
  "groups": [
    {
      "id": "general",
      "label": "General",
      "order": 1
    },
    {
      "id": "wiring",
      "label": "Connections",
      "order": 2
    },
    {
      "id": "timing",
      "label": "Timing",
      "order": 3
    },
    {
      "id": "policy",
      "label": "Host policy",
      "order": 4
    },
    {
      "id": "updates",
      "label": "Game updates",
      "order": 5
    }
  ],
  "fields": [
    {
      "key": "logLevel",
      "env": "Logging__LogLevel__Default",
      "label": "Log level",
      "description": "Minimum severity this leaf logs.",
      "group": "general",
      "type": "enum",
      "values": [
        "Trace",
        "Debug",
        "Information",
        "Warning",
        "Error",
        "Critical"
      ],
      "default": "Information",
      "risk": "safe"
    },
    {
      "key": "kgsmPath",
      "env": "Scheduler__KgsmPath",
      "label": "KGSM executable",
      "description": "Path to the KGSM executable, which the scheduler reads each server's schedule from. It is checked at startup, and the daemon refuses to run if nothing is there.",
      "group": "wiring",
      "type": "path",
      "default": "/usr/bin/kgsm",
      "risk": "wiring"
    },
    {
      "key": "watchdogSocket",
      "env": "Scheduler__WatchdogSocketPath",
      "label": "Watchdog control socket",
      "description": "The watchdog's control socket, which every scheduled restart is issued through. It has to match the path the watchdog listens on, or nothing scheduled ever fires.",
      "group": "wiring",
      "type": "path",
      "default": "/run/kgsm-watchdog/control.sock",
      "risk": "wiring"
    },
    {
      "key": "statusSocket",
      "env": "Scheduler__StatusSocketPath",
      "label": "Status socket",
      "description": "Unix socket the scheduler serves its schedule snapshot on. The Control Panel reads it here to show what is scheduled and when it last ran.",
      "group": "wiring",
      "type": "path",
      "default": "/run/kgsm-scheduler/status.sock",
      "risk": "wiring",
      "pairedApiKey": "Api__SchedulerSocketPath"
    },
    {
      "key": "controlSocket",
      "env": "Scheduler__ControlSocketPath",
      "label": "Control socket",
      "description": "Unix socket the scheduler takes instructions on — postponing a scheduled restart, for instance. Separate from the status socket, which is read-only.",
      "group": "wiring",
      "type": "path",
      "default": "/run/kgsm-scheduler/control.sock",
      "risk": "wiring",
      "pairedApiKey": "Api__SchedulerControlSocketPath"
    },
    {
      "key": "stateDirectory",
      "env": "Scheduler__StateDirectory",
      "label": "State directory",
      "description": "Directory the scheduler keeps its own state in. It remembers which servers have already been told a restart is coming, so a restart of the scheduler does not repeat the warnings or forget that it promised them.",
      "group": "wiring",
      "type": "path",
      "default": "/var/lib/kgsm-scheduler",
      "risk": "wiring"
    },
    {
      "key": "pollIntervalSec",
      "env": "Scheduler__PollIntervalSeconds",
      "label": "Schedule re-scan interval",
      "description": "How often each server's schedule is re-read from KGSM. This bounds how quickly a schedule change takes effect; it does not affect the accuracy of a fire that is already scheduled.",
      "group": "timing",
      "type": "int",
      "default": "60",
      "min": 5,
      "unit": "s",
      "risk": "safe"
    },
    {
      "key": "graceWindowMin",
      "env": "Scheduler__GraceWindowMinutes",
      "label": "Missed-fire grace window",
      "description": "How late a missed maintenance window may be and still run. Anything later is skipped, so a host that was down does not come back to a burst of catch-up work. A window that comes round more often than twice this gets half its own period instead, so a frequent window can never have two occurrences owed at once.",
      "group": "timing",
      "type": "int",
      "default": "10",
      "min": 0,
      "unit": "min",
      "risk": "safe"
    },
    {
      "key": "minWindowPeriodMin",
      "env": "Scheduler__MinimumWindowPeriodMinutes",
      "label": "Minimum window period",
      "description": "How often maintenance is allowed to happen at all on this host. A window asking to run more frequently than this is reported as one that will not fire, rather than firing anyway — the floor is the host's answer, not the server's.",
      "group": "policy",
      "type": "int",
      "default": "10",
      "min": 10,
      "unit": "min",
      "risk": "safe"
    },
    {
      "key": "allowDisruptiveTasks",
      "env": "Scheduler__AllowDisruptiveTasks",
      "label": "Allow disruptive maintenance",
      "description": "Whether maintenance that interrupts the people on a server — a restart, or an update — may run on this host at all. Off leaves backups running as normal; anything disruptive is recorded as skipped and the windows carrying it are not announced, since there would be nothing true to announce.",
      "group": "policy",
      "type": "bool",
      "default": "true",
      "risk": "safe"
    },
    {
      "key": "updateCheckEnabled",
      "env": "Scheduler__UpdateCheckEnabled",
      "label": "Check for game updates",
      "description": "Whether to check each server for a newer game build. Turning this off means nothing on this host ever asks upstream, so no update notification is raised — servers keep running exactly as they are.",
      "group": "updates",
      "type": "bool",
      "default": "true",
      "risk": "safe"
    },
    {
      "key": "updateCheckIntervalMin",
      "env": "Scheduler__UpdateCheckIntervalMinutes",
      "label": "Update check interval",
      "description": "How often every server is checked for a newer build. A game release is not a fast-moving fact and each check costs a real request to the game's upstream, so hourly is generous.",
      "group": "updates",
      "type": "int",
      "default": "60",
      "min": 5,
      "unit": "min",
      "risk": "safe"
    },
    {
      "key": "updateCheckStaggerSec",
      "env": "Scheduler__UpdateCheckStaggerSeconds",
      "label": "Update check stagger",
      "description": "How long to wait between checking one server and the next. Each server asks its own upstream, so this spreads the requests out instead of sending them all in the same second.",
      "group": "updates",
      "type": "int",
      "default": "5",
      "min": 0,
      "unit": "s",
      "risk": "safe"
    }
  ]
}
